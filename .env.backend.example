Code snippet
# Database Configuration
POSTGRES_DB=atomic_db
POSTGRES_USER=atomic_user
POSTGRES_PASSWORD=change_me
SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/atomic_db

# Vault Configuration
VAULT_ENABLED=true
VAULT_URI=http://atomic-vault:8200
VAULT_TOKEN=dev-only-root-token
VAULT_DEV_ROOT_TOKEN_ID=dev-only-root-token
VAULT_KV_BACKEND=secret
VAULT_KV_CONTEXT=atomic-backend

# Security & Cross-Origin Configuration
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000