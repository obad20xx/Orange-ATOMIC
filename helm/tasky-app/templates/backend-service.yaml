apiVersion: v1
kind: Service
metadata:
  name: {{ include "tasky.releaseName" . }}-backend
  labels:
    {{- include "tasky.labels" . | nindent 4 }}
    {{- include "tasky.backend.selectorLabels" . | nindent 4 }}
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tasky.backend.selectorLabels" . | nindent 4 }}
